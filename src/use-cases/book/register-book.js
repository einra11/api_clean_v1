const createBook = ({ bookDB, makeBook_ENTITY }) => {
    return async function add( data ) {
        let result = {}
        //Steps: Use entity validation -> check dulplication from the validated
    }
}