import BookModel from "./model.item"
import UserModel from "./model.user";

const services = Object.freeze({
    BookModel,
    UserModel
})

export default services;

export  {BookModel, UserModel}

